1. HTML (2-3 buổi)
    - Giới thiệu HTML
    - Các thẻ HTML
    - Thuộc tính của thẻ HTML
2. CSS (2-3 buổi)
    - Cách sử dụng CSS
    - Cách tương tác CSS với HTML
    - Các thuộc tính và giá trị của thuộc tính CSS
    - Mức độ ưu tiên trong CSS
    - Responsive
3. Javascript (1 buổi)
    - ngôn ngữ lập trình kịch bản
    + khai báo/gán/sử dụng biến, mảng, object,..
    + cấu trúc điều khiển: if else/ switch case
    + vòng lặp: for/ do while / while/ foreach
    + hàm xử lý chuỗi/ mảng
    - rengular execption: check value
4. DOM/Jquery (2-3 buổi)
    - Dùng để tương tác giữa Javascript và HTML/CSS
5. Bootstrap (1-2 buổi)
    - thư viện giúp code nhanh hơn (HTML,CSS, Javascript)
6. Photoshop (1 buổi)
    - cắt ảnh
    - đo khoảng cách
    - đo bảng màu
    - kiểm tra font chữ và font size
7. Thực hành Project (2-3 buổi)
    - Lấy thiết kế và viết HTML, CSS, Javascript để tạo thành 1 sản phẩm hoàn chỉnh

Bài lab nộp thông qua classroom: nộp bằng link git.
Một số câu lệnh git:
Làm 1 lần:
    git config --global user.name "Your Name"
    git config --global user.email "your.email@example.com"
Kiểm tra:
    git config --global --list

Up file:
    git add .  // git add tên_file // chọn các file muốn up
    git commit -m "nội dung"  // đóng gói các file đưa lên xe và gán ghi chú
    git push // gửi hàng

lấy dữ liệu lần đầu về:
    git clone link_đường_dẫn

cập nhật dữ liệu mới nhất:
    git pull


Một số phím tắt nhanh:
ctrl + C: copy
ctrl + V: paste
ctrl + X : cut
ctrl + Z: quay lại
ctrl + Y: tiến lên

alt + shift + F : format code 
alt + shift + mũi tên lên/xuống : copy
ctrl + D : khoanh các nội dung giống nhau (trỏ/sửa nhiều cùng 1 lúc)
tab : nhảy đến phần code tiếp theo (HTML)
ctrl+/ : comment

tag:  là thẻ HTML 
tag01#tên_id.tên_class>tag02#tên_id.tên_class*n: Tạo n thẻ tag02 nằm bên trong thẻ tag01

VD:
table>thead>tr*số_lượng>th*số_lượng
=> 
